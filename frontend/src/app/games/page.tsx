const GamePage = () => {
  return (
    <div>
      <h1>
        This is the same thing as the Categories page on your ecommrce page, if
        you will have one. So if your client sells a multitude of objects, then
        this is where the categores of them will be listed.
      </h1>
    </div>
  );
};
export default GamePage;
